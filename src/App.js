import UserFinder from './components/UserFinder';

function App() {
  return (
    <div>
      <UserFinder />
    </div>
  );
}

export default App;

// You can't use react hooks in class-based components
